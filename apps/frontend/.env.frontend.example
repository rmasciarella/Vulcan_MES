# =============================================================================
# FRONTEND-SPECIFIC ENVIRONMENT VARIABLES
# Variables used only by the Next.js frontend application
# =============================================================================

# Core Supabase Configuration (Client-Side - Safe for Browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Application Configuration
NEXT_PUBLIC_APP_NAME=Vulcan MES
NEXT_PUBLIC_APP_ENV=development

# Development & Debug Features
# WARNING: Set to false in production environments
NEXT_PUBLIC_ENABLE_DEBUG_UI=false
NEXT_PUBLIC_ENABLE_MOCK_DATA=false

# Health Check Configuration
# Enable database health monitoring for active system status tracking
# NOTE: Consumes database connections - use sparingly in production
NEXT_PUBLIC_ENABLE_DB_HEALTH_CHECK=false

# Health check interval in milliseconds (5 minutes default)
# Lower values increase database load but provide more real-time status
NEXT_PUBLIC_DB_HEALTH_CHECK_INTERVAL_MS=300000

# Maximum backoff interval during failures (1 hour default)
# Prevents overwhelming failing database with excessive retries
NEXT_PUBLIC_DB_HEALTH_CHECK_MAX_INTERVAL_MS=3600000

# =============================================================================
# SERVER-ONLY VARIABLES (Not exposed to browser)
# =============================================================================

# Supabase Service Role Key
# WARNING: This key grants admin access - never expose to client-side code
SUPABASE_SECRET=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =============================================================================
# ENVIRONMENT-SPECIFIC CONFIGURATION EXAMPLES
# =============================================================================

# Development Environment:
# - Debug UI enabled for development tools
# - Health checks enabled with frequent intervals for rapid feedback
# - Mock data disabled (use real backend data)

# Staging Environment:
# - Debug UI may be enabled for testing
# - Health checks enabled with moderate intervals
# - Mock data disabled

# Production Environment:
# - Debug UI MUST be disabled (security/performance)
# - Health checks disabled unless critical monitoring needed
# - Mock data MUST be disabled (data integrity)
# - Use conservative health check intervals if enabled (5+ minutes)

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. NEXT_PUBLIC_* variables are bundled into client-side JavaScript
# 2. SUPABASE_SECRET must never be exposed to browser (server-only)
# 3. Use different Supabase projects for dev/staging/production
# 4. Regularly rotate Supabase keys, especially service role keys
# 5. Monitor for unexpected environment variable usage in application logs