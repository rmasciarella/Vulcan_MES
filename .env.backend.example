# =============================================================================
# BACKEND-SPECIFIC ENVIRONMENT VARIABLES
# Variables used only by the FastAPI backend application
# =============================================================================

# FastAPI Configuration
BACKEND_CORS_ORIGINS=["http://localhost:3000"]

# Admin User Setup
FIRST_SUPERUSER=admin@vulcan.com
FIRST_SUPERUSER_PASSWORD=changeme

# Email Configuration
SMTP_TLS=true
SMTP_PORT=587
SMTP_HOST=smtp.gmail.com
SMTP_USER=admin@vulcan.com
SMTP_PASSWORD=your-smtp-password
EMAILS_FROM_EMAIL=admin@vulcan.com
EMAILS_FROM_NAME=Vulcan MES

# Security & Monitoring
SECRET_KEY=changeme-use-a-secure-key-in-production
SENTRY_DSN=your-sentry-dsn

# Performance & Optimization
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# API Rate Limiting
API_RATE_LIMIT_PER_MINUTE=1000

# Solver Configuration
SOLVER_TIMEOUT_SECONDS=300
MAX_CONCURRENT_SOLVERS=4

# Data Retention
AUDIT_LOG_RETENTION_DAYS=2555
DATA_BACKUP_RETENTION_DAYS=90

# =============================================================================
# ENVIRONMENT-SPECIFIC CONFIGURATION NOTES
# =============================================================================

# Development Environment:
# - Use localhost URLs and test credentials
# - Enable debug logging and detailed error messages
# - Use shorter retention periods for faster testing

# Staging Environment:
# - Use staging database and email services
# - Enable monitoring but with relaxed limits
# - Test production-like configuration

# Production Environment:
# - Use production database and email services
# - Enable all monitoring and security features
# - Use secure, rotated secrets
# - Set appropriate rate limits and timeouts

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. Change all default passwords and keys before deployment
# 2. Use environment-specific values (dev/staging/prod)
# 3. Regularly rotate SECRET_KEY and other sensitive credentials
# 4. Monitor SMTP and Sentry configurations for security
# 5. Review rate limits and solver timeouts for production workloads