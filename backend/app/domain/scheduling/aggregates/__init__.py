"""
Aggregate Roots

Contains aggregate roots that define consistency boundaries within the domain.
Aggregates ensure that business invariants are maintained across related entities
and value objects.

Each aggregate root serves as the single entry point for modifying the entities
within its boundary, ensuring data consistency and enforcing business rules.
"""
